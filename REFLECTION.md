With this assignment, I found myself repeating the header and footer a lot, where these were needed for every single page and had to be the same, only looking different due to the CSS styling of each template. Similarly, this also occurred when trying to make each file follow the shared skeleton, only changing this once the content was entered and when I had to input additional elements like forms, images, and details. It started to be like a routine whenever I made each page, but with some differences in file location, like when I moved certain layout files to the guide folder for the family pages, this assignment got a bit annoying as I tried to make sure everything worked. 

Especially after my first submission, I didn't realize that the CSS styling didn't apply to all of these pages and that some images were missing. Partially, I overlooked the part where the header had to include a logo, but also I forgot to check the file locations and access the images differently for the guide pages when I moved those pages into the guide directory (ex. img src was "../images/___.svg", not "images/___.svg" for guide/*.html). This also applied to the global and vars CSS files, so I had to update them in a similar fashion to make sure all the required pictures were there, and that the styling and header/footer were consistent throughout all pages.

If I had a tool for this assignment, I would have liked to have a tool that provides a suggested file location, updating it based on what directory I'm editing in. Additionally, some sort of layout generator could be nice too, helping me speed up my process where I don't have to constantly check if any of my files don't follow the shared skeleton.